# count the occurences of letters/alphabets from the given string
input_value = str(input("Enter a value:")).lower()
dict_1= {}
for i in input_value:
    if i not in dict_1:
        dict_1[i]=1
    else:
        dict_1[i]+=1
print(dict_1)
